{
  "name": "hello-vuejs",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/**",
    "workshops/**"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present"
  }
}
